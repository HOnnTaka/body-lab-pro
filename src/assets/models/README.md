# 模型文件目录

## 使用说明

模型文件已移至 `public/models/` 目录。

Vite 会自动将 `public` 目录下的文件复制到构建输出目录，可以通过根路径访问。

### 文件位置
- 开发环境：`public/models/body_simple_origin.glb`
- 访问路径：`/models/body_simple_origin.glb`

### 注意事项
- H5 端使用 public 目录下的本地文件
- 小程序端使用 CDN + 本地缓存
- 文件大小约 4MB
